<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_location_search" model="ir.ui.view">
        <field name="name">stock.location.search</field>
        <field name="model">stock.location</field>
        <field name="arch" type="xml">
            <search string="Stock Locations">
                <field name="complete_name" string="Stock Locations" />
                <filter name="in_location" string="Internal"
                    domain="[('usage', '=', 'internal')]"
                    help="Internal Locations" />
                <filter name="customer" string="Customer"
                    domain="[('usage', '=', 'customer')]"
                    help="Customer Locations" />
                <filter name="prod_inv_location" string="Production"
                    domain="[('usage', 'in', ['inventory', 'production'])]"
                    help="Production Locations" />
                <filter name="supplier" string="Vendor"
                    domain="[('usage', '=', 'supplier')]"
                    help="Vendor Locations" />
                <separator />
                <filter name="empty_location" string="Empty Locations"
                    domain="[('is_empty', '=', True)]" />
                <field name="location_id" string="Parent Location" />
                <field name="usage" string="Location Type" />
                <field name="warehouse_id" string="Warehouse" />
                <separator />
                <filter name="inactive" string="Archived"
                    domain="[('active', '=', False)]" />
                <group string="Group By" expand="0">
                    <filter name="warehouse" string="Warehouse"
                        domain="[]"
                        context="{'group_by': 'warehouse_id'}" />
                    <filter name="usage" string="Location Type"
                        domain="[]"
                        context="{'group_by': 'usage'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_location_tree2" model="ir.ui.view">
        <field name="name">stock.location.list</field>
        <field name="model">stock.location</field>
        <field name="priority" eval="2" />
        <field name="arch" type="xml">
            <list string="Stock Location" decoration-info="usage=='view'" multi_edit="1">
                <field name="company_id" column_invisible="True" />
                <field name="active" column_invisible="True" />
                <field name="complete_name" string="Location" />
                <field name="usage" />
                <field name="is_empty" invisible="usage not in ['internal', 'transit']" />
                <field name="storage_category_id"
                    readonly="usage != 'internal'"
                    groups="stock.group_stock_multi_locations" />
                <field name="company_id"
                    groups="base.group_multi_company" />
            </list>
        </field>
    </record>

    <record id="stock_location_view_tree2_editable" model="ir.ui.view">
        <field name="name">stock.location.list2.editable</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2" />
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="create">false</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_location_form" model="ir.ui.view">
        <field name="name">stock.location.form</field>
        <field name="model">stock.location</field>
        <field name="arch" type="xml">
            <form string="Stock Location">
                <field name="company_id" invisible="1" />
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button name="%(location_open_putaway)d" string="Putaway Rules"
                            type="action" class="oe_stat_button"
                            icon="fa-random"
                            context="{'default_company_id': company_id}"
                            groups="stock.group_stock_multi_locations" />
                        <button name="stock.act_product_location_open" string="Products"
                            icon="fa-th-list"
                            type="action" class="oe_stat_button" />
                        <button name="%(location_open_quants)d" string="Current Stock"
                            type="action" class="oe_stat_button"
                            icon="fa-cubes" />
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger"
                        invisible="active" />
                    <label for="name" />
                    <h1>
                        <field name="name" placeholder="e.g. Spare Stock" />
                    </h1>
                    <label for="location_id" />
                    <h2>
                        <field name="location_id" placeholder="e.g. Physical Locations"
                            options="{'no_create': True}" />
                    </h2>
                    <group>
                        <group name="additional_info" string="Additional Information">
                            <field name="active" invisible="1" />
                            <field name="usage" />
                            <field name="storage_category_id"
                                invisible="usage != 'internal'"
                                groups="stock.group_stock_multi_locations" />
                            <field name="company_id"
                                options="{'no_create': True}"
                                groups="base.group_multi_company" />
                            <field name="scrap_location"
                                invisible="usage not in ('inventory', 'internal')" />
                            <field name="replenish_location"
                                invisible="usage != 'internal'" />
                        </group>
                        <group string="Cyclic Counting"
                            invisible="usage not in ('internal', 'transit') or not company_id">
                            <label for="cyclic_inventory_frequency" string="Inventory Frequency" />
                            <div class="o_row">
                                <field name="cyclic_inventory_frequency"
                                    nolabel="1"
                                    class="oe_inline" />
                                <span>days</span>
                            </div>
                            <field name="last_inventory_date" />
                            <field name="next_inventory_date" invisible="not active" />
                        </group>
                        <group string="Logistics" groups="stock.group_adv_location">
                            <field name="removal_strategy_id"
                                widget="radio"
                                options="{'no_create': True}"
                                invisible="usage in ('supplier', 'customer', 'inventory', 'production') or scrap_location or usage == 'transit' and not company_id" />
                        </group>
                    </group>
                    <field name="comment" placeholder="External note..." />
                </sheet>
            </form>
        </field>
    </record>

    <record id="stock_location_view_form_editable" model="ir.ui.view">
        <field name="name">stock.location.form.editable</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form" />
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="create">false</attribute>
            </xpath>
        </field>
    </record>

    <record id="action_storage_category_locations" model="ir.actions.act_window">
        <field name="name">Locations</field>
        <field name="res_model">stock.location</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('storage_category_id', '=', active_id)]</field>
    </record>

    <record id="action_location_form" model="ir.actions.act_window">
        <field name="name">Locations</field>
        <field name="res_model">stock.location</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_location_tree2" />
        <field name="search_view_id" ref="view_location_search" />
        <field name="context">{'search_default_in_location':1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add a new location
            </p>
            <p>
                Define your locations to reflect your warehouse structure and
                organization. Odoo is able to manage physical locations
                (warehouses, shelves, bin, etc), partner locations (customers,
                vendors) and virtual locations which are the counterpart of
                the stock operations like the manufacturing orders
                consumptions, inventories, etc.
            </p>
            <p>
                Every stock operation in Odoo moves the products from one
                location to another one. For instance, if you receive products
                from a vendor, Odoo will move products from the Vendor
                location to the Stock location. Each report can be performed on
                physical, partner or virtual locations.
            </p>
        </field>
    </record>

    <record id="action_prod_inv_location_form" model="ir.actions.act_window">
        <field name="name">Locations</field>
        <field name="res_model">stock.location</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_location_tree2" />
        <field name="search_view_id" ref="view_location_search" />
        <field name="context">{'search_default_prod_inv_location': 1}</field>
    </record>
</odoo>