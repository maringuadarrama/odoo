<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_search_form_view" model="ir.ui.view">
        <field name="name">product.product.search</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="product.product_template_search_view" />
        <field name="arch" type="xml">
            <field name="product_tag_ids" position="replace">
                <field name="all_product_tag_ids" string="Tags" />
            </field>
            <field name="name" position="replace">
                <field name="name" string="Product"
                    filter_domain="['|', '|', ('default_code', 'ilike', self), ('name', 'ilike', self), ('barcode', 'ilike', self)]" />
            </field>
            <field name="attribute_line_ids" position="replace">
                <field name="product_template_attribute_value_ids"
                    groups="product.group_product_variant" />
                <field name="product_tmpl_id" string="Product Template" />
            </field>
            <filter name="group_by_categ_id" position="after">
                <filter name="group_by_product_tmpl_id" string="Product Template"
                    context="{'group_by':'product_tmpl_id'}" />
            </filter>
        </field>
    </record>

    <record id="product_view_search_catalog" model="ir.ui.view">
        <field name="name">product.view.search.catalog</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <search string="Product">
                <field name="name" string="Product"
                    filter_domain="['|', '|', ('default_code', 'ilike', self), ('name', 'ilike', self), ('barcode', 'ilike', self)]" />
                <field name="categ_id"
                    filter_domain="[('categ_id', 'child_of', raw_value)]" />
                <field name="product_template_attribute_value_ids"
                    groups="product.group_product_variant" />
                <field name="product_tmpl_id" string="Product Template" />
                <filter name="favorites" string="Favorites"
                    domain="[('is_favorite', '=', True)]" />
                <separator />
                <filter name="services" string="Services"
                    domain="[('type', '=', 'service')]" />
                <filter name="goods" string="Goods"
                    domain="[('type', '=', 'consu')]" />
                <group expand="1" string="Group By">
                    <filter string="Product Type" name="type"
                        context="{'group_by':'type'}" />
                    <filter string="Product Category" name="categ_id"
                        context="{'group_by':'categ_id'}" />
                </group>
                <searchpanel>
                    <field name="categ_id" string="Product Category"
                        icon="fa-th-list" />
                    <field name="product_template_attribute_value_ids" string="Attributes"
                        icon="fa-th-list"
                        domain="[('ptav_active', '=', True), ('product_tmpl_id.active', '=', True)]"
                        enable_counters="1"
                        select="multi" />
                </searchpanel>
            </search>
        </field>
    </record>

    <record id="product_product_tree_view" model="ir.ui.view">
        <field name="name">product.product.list</field>
        <field name="model">product.product</field>
        <field eval="7" name="priority" />
        <field name="arch" type="xml">
            <list string="Product Variants" multi_edit="1" duplicate="false" sample="1"
                default_order="is_favorite desc, default_code, name, id">
                <field name="is_favorite" widget="boolean_favorite" nolabel="1" readonly="1" />
                <field name="default_code" optional="show" readonly="1" />
                <field name="barcode" optional="hide" readonly="1" />
                <field name="name" readonly="1" />
                <field name="product_template_variant_value_ids" widget="many2many_tags"
                    readonly="1" groups="product.group_product_variant" />
                <field name="company_id" readonly="1"
                    optional="hide"
                    groups="base.group_multi_company" />
                <field name="lst_price" string="Sales Price" optional="show" />
                <field name="standard_price" optional="show" />
                <field name="categ_id" optional="hide" />
                <field name="product_tag_ids" widget="many2many_tags" readonly="1" optional="hide" />
                <field name="additional_product_tag_ids" widget="many2many_tags" optional="hide" />
                <field name="type" readonly="1" optional="hide" />
                <field name="uom_id"
                    readonly="1"
                    optional="show"
                    groups="uom.group_uom" />
                <field name="product_tmpl_id" readonly="1" column_invisible="True" />
                <field name="active" column_invisible="True" />
            </list>
        </field>
    </record>

    <record id="product_variant_easy_edit_view" model="ir.ui.view">
        <field name="name">product.product.view.form.easy</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <form string="Variant Information" duplicate="false">
                <sheet>
                    <div class="oe_button_box" name="button_box" />
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger"
                        invisible="active" />
                    <field name="active" invisible="1" />
                    <field name="id" invisible="1" />
                    <field name="company_id" invisible="1" />
                    <field name="image_1920" widget="image" class="oe_avatar"
                        options="{'convert_to_webp': True,'preview_image': 'image_128'}" />
                    <div class="oe_title">
                        <label for="name" string="Product Name" />
                        <h1>
                            <field name="name"
                                placeholder="e.g. Odoo Enterprise Subscription" readonly="1" />
                        </h1>
                        <field name="product_template_attribute_value_ids" widget="many2many_tags"
                            readonly="1" />
                        <p>
                            <span>All general settings about this product are managed on</span>
                            <button name="action_open_product_template" string="the product template."
                                type="object" class="oe_link oe_link_product ps-0 ms-1 mb-1" />
                        </p>
                    </div>
                    <group>
                        <group name="codes" string="Codes">
                            <field name="default_code" />
                            <field name="barcode" />
                            <field name="type" invisible="1" />
                        </group>
                        <group name="pricing" string="Pricing">
                            <field name="count_product_variant" invisible="1" />
                            <label for="lst_price" string="Sales Price" />
                            <div class="o_row">
                                <field name="lst_price" widget='monetary'
                                    class="oe_inline"
                                    readonly="count_product_variant &gt; 1"
                                    options="{'currency_field': 'currency_id', 'field_digits': True}" />
                            </div>
                            <label for="standard_price" />
                            <div class="o_row">
                                <field name="standard_price" widget="monetary"
                                    class="oe_inline"
                                    options="{'currency_field': 'cost_currency_id', 'field_digits': True}" />
                            </div>
                            <field name="currency_id" invisible='1' />
                            <field name="cost_currency_id" invisible="1" />
                        </group>
                    </group>
                    <group>
                        <group name="weight" string="Logistics" invisible="type != 'consu'">
                            <label for="volume" />
                            <div class="o_row">
                                <field name="volume" class="oe_inline" />
                                <span>
                                    <field name="volume_uom_name" />
                                </span>
                            </div>
                            <label for="weight" />
                            <div class="o_row">
                                <field name="weight" class="oe_inline" />
                                <span>
                                    <field name="weight_uom_name" />
                                </span>
                            </div>
                        </group>
                        <group name="sales" string="Sales">
                            <field name="uom_ids" widget="many2many_uom_tags"
                                options="{'no_quick_create': True, 'edit_tags': True}"
                                force_save="1"
                                context="{'product_id': id}"
                                groups="uom.group_uom" />
                            <field name="product_tag_ids" widget="many2many_tags" readonly="1" />
                            <field name="additional_product_tag_ids" widget="many2many_tags"
                                context="{'product_variant_id': id}" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="product_product_view_tree_tag" model="ir.ui.view">
        <field name="name">product.product.view.list.tag</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <list string="Product Variants" editable="bottom"
                default_order="is_favorite desc, default_code, name, id">
                <field name="name" readonly="1" />
                <field name="default_code" readonly="1" optional="show" />
                <field name="product_template_variant_value_ids" widget="many2many_tags"
                    readonly="1"
                    groups="product.group_product_variant" />
                <field name="description" readonly="1" optional="show" />
            </list>
        </field>
    </record>

    <record id="product_kanban_view" model="ir.ui.view">
        <field name="name">Product Kanban</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <kanban sample="1" default_order="is_favorite desc, default_code, name, id">
                <field name="activity_state" />
                <field name="color" />
                <progressbar field="activity_state"
                    colors='{"planned": "success", "today": "warning", "overdue": "danger"}' />
                <templates>
                    <t t-name="card" class="row g-0">
                        <main class="col-10 pe-2">
                            <div class="d-flex mb-1 h5">
                                <field name="is_favorite" widget="boolean_favorite"
                                    class="me-1" nolabel="1" />
                                <field name="name" />
                            </div>
                            <span t-if="record.default_code.value"> [<field name="default_code" />] </span>
                            <field name="product_template_variant_value_ids" widget="many2many_tags"
                                options="{'color_field': 'color'}"
                                groups="product.group_product_variant" />
                            <span> Price: <field name="lst_price"></field>
                            </span>
                        </main>
                        <aside class="col-2">
                            <field name="image_128" widget="image"
                                options="{'img_class': 'o_image_64_contain mw-100'}" alt="Product" />
                        </aside>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="product_view_kanban_catalog" model="ir.ui.view">
        <field name="name">product.view.kanban.catalog</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <kanban records_draggable="0" js_class="product_kanban_catalog"
                default_order="is_favorite desc, default_code, name, id">
                <field name="id" />
                <templates>
                    <t t-name="menu">
                        <div class="o_product_catalog_cancel_global_click">
                            <a role="menuitem" type="open" class="dropdown-item border-top-0">Edit</a>
                        </div>
                    </t>
                    <t t-name="card" class="p-0">
                        <!-- TODO : not getting border when select record -->
                        <div class="d-flex flex-grow-1 m-2">
                            <div class="p-2 pt-1 d-flex flex-column m-0"
                                t-attf-id="product-{{record.id.raw_value}}">
                                <div class="d-flex">
                                    <field name="is_favorite" widget="boolean_favorite"
                                        nolabel="1"
                                        class="mt-1 me-1"
                                        readonly="1" />
                                    <field name="name" class="fw-bolder fs-4 text-reset mb-1" />
                                </div>
                                <div t-if="record.default_code.value"> [<field name="default_code" />
                                    ] </div>
                                <!-- Used by @web/product/js/product_catalog/order_line to
                                        show the price using a t-portal. -->
                                <div name="o_kanban_price"
                                    t-attf-id="product-{{record.id.raw_value}}-price" />
                                <field name="product_template_attribute_value_ids" widget="many2many_tags"
                                    domain="[('id', 'in', parent.ids)]"
                                    options="{'color_field': 'color'}"
                                    groups="product.group_product_variant" />
                            </div>
                            <field name="image_128" widget="image"
                                alt="Product"
                                class="ms-auto"
                                options="{'size': [55, 55]}"
                                invisible="not image_128" />
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="product_normal_form_view" model="ir.ui.view">
        <field name="name">product.product.form</field>
        <field name="model">product.product</field>
        <field name="mode">primary</field>
        <field eval="7" name="priority" />
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="string">Product Variant</attribute>
                <attribute name="duplicate">false</attribute>
            </form>
            <xpath expr="//div[@name='standard_price_uom']" position="after">
                <field name="default_code" />
                <field name="barcode" />
            </xpath>
            <xpath expr="//field[@name='is_favorite']" position="attributes">
                <attribute name="readonly">1</attribute>
            </xpath>
            <field name="list_price" position="attributes">
                <attribute name="invisible">1</attribute>
                <attribute name="readonly">count_product_variant &gt; 1</attribute>
            </field>
            <label for="list_price" position="replace">
                <label for="lst_price" />
            </label>
            <field name="list_price" position="after">
                <field name="lst_price" widget='monetary'
                    class="oe_inline"
                    options="{'currency_field': 'currency_id', 'field_digits': True}" />
            </field>
            <field name="name" position="after">
                <field name="product_tmpl_id"
                    required="id"
                    readonly="1"
                    class="oe_inline"
                    invisible="1" />
            </field>
            <xpath expr="//div[hasclass('oe_title')]" position="inside">
                <field name="product_template_variant_value_ids" widget="many2many_tags"
                    readonly="1"
                    invisible="not product_template_variant_value_ids"
                    groups="product.group_product_variant" />
            </xpath>
            <field name="product_tag_ids" position="attributes">
                <attribute name="options">{'no_open': True}</attribute>
                <attribute name="context">{'product_template_id': product_tmpl_id}</attribute>
            </field>
            <field name="product_tag_ids" position="after">
                <field name="additional_product_tag_ids" widget="many2many_tags" />
            </field>
            <field name="uom_ids" position="attributes">
                <attribute name="context">{'product_id': id}</attribute>
            </field>
        </field>
    </record>

    <record id="product_product_view_form_normalized" model="ir.ui.view">
        <field name="name">product.product.view.form.normalized</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="image_1920" widget="image"
                        nolabel="1"
                        class="oe_avatar"
                        options="{'convert_to_webp': True,'preview_image': 'image_128'}" />
                    <group name="name">
                        <field name="company_id" invisible="1" />
                        <field name="currency_id" invisible="1" />
                        <field name="cost_currency_id" invisible="1" />
                        <field name="name" string="Product Name"
                            placeholder="e.g. Cheese Burger"
                            class="oe_inline" />
                        <field name="barcode" placeholder="e.g. 1234567890" class="oe_inline" />
                        <field name="list_price" widget="monetary"
                            class="oe_inline"
                            options="{'currency_field': 'currency_id', 'field_digits': True}" />
                        <field name="description" invisible="1" />  <!-- Hide the description field
                        for set data in product barcodelookup -->
                        <field name="weight" invisible="1" />       <!-- Hide the weight field for set data
                        in product barcodelookup -->
                        <field name="categ_id" invisible="1" />     <!-- Hide the categ_id field set data
                        in product barcodelookup -->
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="product_product_view_activity" model="ir.ui.view">
        <field name="name">product.product.activity</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <activity string="Product Variants">
                <field name="id" />
                <field name="default_code" />
                <templates>
                    <div t-name="activity-box">
                        <img
                            t-att-src="activity_image('product.product', 'image_128', record.id.raw_value)"
                            role="img" t-att-title="record.id.value" t-att-alt="record.id.value" />
                        <div class="ms-2">
                            <field name="name" display="full" class="o_text_block" />
                            <div t-if="record.default_code.value" class="o_text_block text-muted"> [<field
                                    name="default_code" />] </div>
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>

    <record id="product_normal_action" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">list,form,kanban,activity</field>
        <field name="search_view_id" ref="product_search_form_view" />
        <field name="view_id" eval="False" /> <!-- Force empty -->
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product variant
            </p>
            <p>
                You must define a product for everything you sell or purchase,
                whether it's a storable product, a consumable or a service.
            </p>
        </field>
    </record>

    <record id="product_variant_action" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="res_model">product.product</field>
        <field name="search_view_id" ref="product_search_form_view" />
        <field name="view_ids"
            eval="[
                (5, 0, 0),
                (0, 0, {'view_mode': 'list'}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('product_variant_easy_edit_view')}),
                (0, 0, {'view_mode': 'kanban'})
            ]" />
        <field name="context">{
            'search_default_product_tmpl_id': [active_id],
            'default_product_tmpl_id': active_id,
            'create': False
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product variant
            </p>
            <p>
                You must define a product for everything you sell or purchase,
                whether it's a storable product, a consumable or a service.
                The product form contains information to simplify the sale process:
                price, notes in the quotation, accounting data, procurement methods, etc.
            </p>
        </field>
    </record>

    <record id="product_normal_action_sell" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">kanban,list,form,activity</field>
        <field name="context">{"search_default_filter_to_sell":1}</field>
        <field name="view_id" ref="product_product_tree_view" />
        <field name="search_view_id" ref="product_search_form_view" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product variant
            </p>
            <p>
                You must define a product for everything you sell, whether it's a physical product,
                a consumable or a service you offer to customers.
                The product form contains information to simplify the sale process:
                price, notes in the quotation, accounting data, procurement methods, etc.
            </p>
        </field>
    </record>

    <record id="action_product_print_labels" model="ir.actions.server">
        <field name="name">Print Labels</field>
        <field name="model_id" ref="product.model_product_product" />
        <field name="binding_model_id" ref="product.model_product_product" />
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_open_label_layout()
        </field>
    </record>

    <record id="action_product_price_list_report" model="ir.actions.server">
        <field name="name">Pricelist Report</field>
        <field name="model_id" ref="product.model_product_product" />
        <field name="binding_model_id" ref="product.model_product_product" />
        <field name="state">code</field>
        <field name="code">
action = {
    'name': 'Pricelist Report',
    'type': 'ir.actions.client',
    'tag': 'generate_pricelist_report',
    'context': env.context,
}
        </field>
        <field name="group_ids" eval="[(4, ref('group_product_pricelist'))]" />
    </record>

    <!-- Precedence -->
    <record id="product_template_only_form_view" model="ir.ui.view">
        <field name="name">product.template.product.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="mode">primary</field>
        <field name="priority" eval="8" />
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="name">Product Template</attribute>
            </xpath>
            <field name="categ_id" position="after">
                <field name="default_code" string="Reference"
                    invisible="count_product_variant &gt; 1" />
                <field name="valid_product_template_attribute_line_ids" invisible="1" />
                <field name="barcode"
                    invisible="count_product_variant &gt; 1
                        or (count_product_variant == 0
                        and valid_product_template_attribute_line_ids)
                        or type in ['service', 'combo']" />
            </field>
            <button name="action_open_documents" position="before">
                <button name="%(product.product_variant_action)d"
                    type="action" class="oe_stat_button"
                    icon="fa-sitemap"
                    invisible="count_product_variant &lt;= 1"
                    groups="product.group_product_variant">
                    <field name="count_product_variant" string="Variants" widget="statinfo" />
                </button>
            </button>
            <xpath expr="//page[@name='general_information']" position="after">
                <page name="variants" string="Attributes &amp; Variants"
                    invisible="type == 'combo'"
                    groups="product.group_product_variant">
                    <field name="attribute_line_ids" widget="one2many"
                        context="{'show_attribute': False}">
                        <list string="Variants" editable="bottom"
                            decoration-info="value_count &lt;= 1">
                            <field name="value_count" column_invisible="True" />
                            <field name="sequence" widget="handle" />
                            <field name="attribute_id" readonly="id" />
                            <field name="value_ids" widget="many2many_tags"
                                options="{'no_create_edit': True, 'color_field': 'color'}"
                                context="{'default_attribute_id': attribute_id, 'show_attribute': False}" />
                            <button name="action_open_attribute_values" string="Configure"
                                type="object" class="float-end btn-secondary"
                                groups="product.group_product_variant" />
                        </list>
                    </field>
                    <p class="opacity-50 oe_edit_only">
                        <strong>Warning</strong>: adding or deleting attributes will delete and
                        recreate existing variants and lead to the loss of their possible
                        customizations. </p>
                </page>
            </xpath>
            <xpath expr="//group[@name='group_standard_price']" position="after">
                <field name="product_properties" columns="2" />
            </xpath>
        </field>
    </record>
</odoo>
